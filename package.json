{
  "name": "@machine-web/mcp",
  "version": "0.1.0",
  "description": "Expose machine-consumable web endpoints for AI agents.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "mcp": "dist/cli/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "mcp.config.js"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-schema.js",
    "prepare": "npm run build",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node tests/smoke.test.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "cheerio": "^1.0.0-rc.12",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.6.3",
    "@types/express": "^4.17.21",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}